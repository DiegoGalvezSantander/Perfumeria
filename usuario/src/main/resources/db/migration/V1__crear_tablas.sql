CREATE TABLE USUARIO (
    id_usuario BIGINT AUTO_INCREMENT PRIMARY KEY,
    nombre_usuario VARCHAR(255),
    email_usuario VARCHAR(255),
    fono_usuario VARCHAR(255),
    run_usuario VARCHAR(255)
);